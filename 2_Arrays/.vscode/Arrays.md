Globally arrays are of size 10^7 but inside the main block it of size 10^6. 
n-1 is the last index
index starts from 0 
Garbage values are assigned to after assignment of array . 

Question 1 --> Largest Element
largest.java --> On sorting the array , the time complexity will be (0(Nlogn)) . 
largest_optimal.java --> Most optimal approach to find the largest element . TC will be 0(N) which is far better than the O(nlogn).
